// let score = "hitesh"
// in case of null it is showing 0
// // in case of true it is showing 1
// // in case of false it is showing 0
// // In case of string it is showing Nan , because it is not a number 

// console.log(typeof score);
// let valueInNumber = Number(score) // Don't rely on this because sometime we don't know do variable contains exactly number
// console.log(valueInNumber);


let isLoggedIn = false

let booleanIsLoggedIn = Boolean(isLoggedIn)
console.log(booleanIsLoggedIn)

//1 => true; 0 => false
// "" => false
// "hitesh" => true

let someNumber = 33
let stringNumber = String(someNumber)
console.log(typeof stringNumber)