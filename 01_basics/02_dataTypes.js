"use strict"; //treat all JS code as newer version

// alert("hello") // this is used in browser, because js engine hidden in browser(we are using nodejs, not browser)

// console.log(3 + 3); console.log("pp") - Don't do this, this is against the code readibility

let name = "Prince"
let age = 18
let isLogged = false
let state;


/* Type of dataTypes in Js 
number => 2 to power 53
bigint => used in trading or last storage
string =>""
boolean => true/false
null => standalone value(it is a type of object)
undefined => when you can't assigned the value, only define the variable
symbol=> used in react
object 
*/

console.log(typeof(age))
console.log(typeof(null))
